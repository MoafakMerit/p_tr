{
  "collectionName": "components_currency_currencies",
  "info": {
    "displayName": "currency",
    "icon": "comment-dollar",
    "description": ""
  },
  "options": {},
  "attributes": {
    "fromCurrency": {
      "type": "enumeration",
      "enum": [
        "USD",
        "TRY",
        "EUR"
      ],
      "required": true
    },
    "toCurrency": {
      "type": "enumeration",
      "enum": [
        "USD",
        "TRY",
        "EUR"
      ],
      "required": true
    }
  }
}
